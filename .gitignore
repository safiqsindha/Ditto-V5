__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.env
.env.save
.venv/
*.log
.DS_Store
.claude/

# Legacy fortnite scratch scripts (replaced by PUBG per A2; kept locally for
# context but not committed since they're not on the active code path).
scripts/fortnite_smoke_test.py
scripts/fetch_fortnite_match_ids.py
scripts/inspect_fortnite_*.py

# Generated event streams — regenerable from pipelines.run() with mock data
# or from real data acquisition with credentials. Not committed to keep repo
# size manageable. Recreate via: python -m run_pilot or pipeline.run().
data/raw/**/*
!data/raw/**/.gitkeep
data/processed/**/*
!data/processed/**/.gitkeep
data/events/**/*
!data/events/**/.gitkeep

# Pytest cache
.pytest_cache/

# Pilot result outputs
RESULTS/*.json

# Archived pre-Phase-D iteration outputs (moved out of top-level RESULTS/
# during 2026-04-30 cleanup; kept locally for context but not committed)
RESULTS/_archive_pre_phase_d/

# Coverage
.coverage
coverage.xml
htmlcov/
