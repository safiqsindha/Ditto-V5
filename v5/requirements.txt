# v5 core
numpy>=1.26.0
scipy>=1.12.0
pandas>=2.2.0
pydantic>=2.6.0
python-dotenv>=1.0.0
tqdm>=4.66.0
requests>=2.31.0
httpx>=0.27.0

# Statistical
statsmodels>=0.14.0

# NBA cell
nba_api>=1.4.0

# CS:GO/CS2 cell
awpy>=1.5.0

# Hearthstone cell
hearthstone>=2.2.0

# Serialization / event storage
jsonlines>=4.0.0
pyarrow>=15.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Notebooks (optional)
jupyter>=1.0.0

# Notes on per-cell external dependencies (not pip-installable):
# Fortnite: xNocken/replay-downloader (Node.js) + SL-x-TnT/FortniteReplayDecompressor (.NET)
# Rocket League: rrrocket (Rust binary) OR carball>=2.0 (Python, wraps rrrocket)
# CS:GO: awpy requires Go 1.21+ for demoinfocs-golang (bundled via cgo in awpy wheels)
